services:
  src:
    build:
      context: .
      args: 
        - DEV=true
    ports:
      - 8000:8000
    volumes:
      - ./src:/src
    command: >
      sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    environment:
      - DEBUG=1
